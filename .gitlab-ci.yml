image: node

stages:
  - build
  - lint

eslint:
  tags:
    - docker
  stage: lint
  script:
    - npm install -g lerna
    - lerna bootstrap --hoist
    - npm run lint
build_server:
  tags:
    - docker
  stage: build
  script:
    - cd /server
    - dotnet build
